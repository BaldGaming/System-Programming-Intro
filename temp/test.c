#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>




int main()
{
  int i;
  for (i=1 ; i<16; i++)
    printf("    Add16 (a=in[%d], b=, out=out[%d]);\n", i, i);

  return 0;
}

// NAME, ID, ADDRESS